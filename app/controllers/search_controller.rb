class SearchController < ApplicationController
  def search
    #search_term = params[:q]
    #
    #redirect_to :root and return unless search_term.present?
    #
    #@artists = User.search(search_term + '*').first(9)
    #@songs = Song.with_user.search(search_term + '*').compact.select { |song| song.released? && song.original? && can?(:read, song) }.first(9)
    #@subtitle = search_term

    # sidebar_for_frontpage
  end  
end

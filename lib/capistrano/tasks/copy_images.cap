namespace :deploy do
  desc "compiles assets locally then rsyncs"
  task :copy_images do
    on roles(:app) do |role|


      #execute "cp -R #{fetch(:deploy_to)}/current/app/assets/images/* #{fetch(:deploy_to)}/current/public/assets"
      #execute "cp -R #{fetch(:deploy_to)}/current/app/assets/flash/dta/* #{fetch(:deploy_to)}/current/public/uploads"
      #execute "cp -R #{fetch(:deploy_to)}/current/app/assets/flash/dtap/* #{fetch(:deploy_to)}/current/public/uploads"
      

    end
  end
end